# Daily Memory — 2026-04-26

### 12:45

## Slash command bus implementation (td-81bf36)

### What was done:
- **pi-mobile + pi-web-dashboard**: Added `/command` autocomplete dropdown with source badges (extension=purple, skill=green, prompt=amber), command routing via event bus for extension commands, disk expansion for skills/templates
- **Event bus pattern**: `pi.events.emit("command:<name>", { args })` for extension commands, `pi.sendMessage({ customType: "command_result" })` for web/mobile feedback (no LLM wake-up)
- **13 extensions with event bus listeners**: pi-workon, pi-webserver, pi-heartbeat, pi-kysely, pi-cron, pi-brave-search, pi-projects, pi-jobs, pi-logger, pi-telemetry, pi-openrouter, pi-cmux, pi-channels
- **3 extensions with partial support**: pi-gmail (status only), pi-personal-crm (all 3 commands)
- **TS7006 fixes**: Added explicit types to handler params across all touched extensions

### Skipped (needs complex ctx):
- pi-context (ctx.getContextUsage), pi-prism (ctx.hasUI), pi-model-router (ctx.hasUI/model/modelRegistry), pi-tts (ctx.sessionManager)
- pi-a2a (198-line handler), pi-github (registerDualCommand pattern), pi-myfinance (14 commands)

### Key decisions:
- `ctx.ui.notify()` → `pi.sendMessage({ customType: "command_result" })` for web/mobile (no triggerTurn)
- Commands that need agent action (like /workon) use `pi.sendUserMessage()` in the event bus handler
- `ctx.ui.setStatus()` → no-op in event bus (TUI-only)
- Source badges differentiate skill/prompt/extension in autocomplete dropdown

### Branch: `td-81bf36/slash-command-bus`
